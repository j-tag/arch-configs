# modeprobe.d

This config file (`jtag.conf`) is created to add NVIDIA DRM modules to kernel. If you don't use an NVIDIA graphic card or you set the modules using initramfs, then you don't need this file.
