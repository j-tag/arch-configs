# Pacman Hooks

In this directory we can store our custom pacman hooks. The `nvidia.hook` custom hook is to rebuild initramfs when `nvidia` package is updated. You need this hook if you added NVIDIA modules to `mkinitcpio.conf` file.
