# Docker Related Commands

This directory conttains commands related to Docker.

## prune.fish

This command will remove all dangling and unused containers, images, networks, volums etc.

Huge help for freeing up space.
